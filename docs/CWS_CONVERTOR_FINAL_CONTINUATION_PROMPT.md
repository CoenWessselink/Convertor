# CWS Convertor final continuation prompt

Continue from the unified three-phase authority and the current source tree. Read `00_START_HERE/CWS_CONVERTOR_CURRENT_AUTHORITY.md`, `validation/phases/ALL_PROMPTS_TRACEABILITY.json`, the three phase checklists and `release/phase3/PHASE_3_RELEASE_MANIFEST.json`.

Preserve one canonical project, one application context, one Viewer host, one Workbench write path, one drawing engine, one manufacturing truth and one export/release gate. Do not reintroduce UI mutation, duplicate parsers, raster production drawings, silent feature loss or silent scope widening.

Treat physical machine/controller qualification and code signing as external evidence. Keep all four machine safety flags false until an independently approved qualification package exists.
